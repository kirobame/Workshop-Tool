using System;
using System.Collections;
using UnityEngine;

[CreateAssetMenu(fileName = "NewToken", menuName = "Custom/Management/Token")]
// Empty asset which represents a key.
// In most cases this key can be used to access runtime or scene instances. 
public class Token : ScriptableObject { }